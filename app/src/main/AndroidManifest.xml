<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nyc.informationforassessments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sharedPrefrences.SharedPrefrencesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Intents.IntentActivityTwo" />
        <activity android:name=".Intents.IntentActivityOne" />
        <activity android:name=".recyclerView.RecyclerViewActivity" />
        <activity android:name=".LifeCycleActivity"></activity>
    </application>

</manifest>